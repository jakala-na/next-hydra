{
  "name": "create-next-hydra",
  "version": "0.3.0",
  "description": "CLI to compose applications from the Next Hydra registry",
  "keywords": [
    "cli",
    "create-next-app",
    "create-next-hydra",
    "next-hydra",
    "scaffold",
    "starter",
    "turborepo"
  ],
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/jakala-na/next-hydra",
    "directory": "packages/create-next-hydra"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./reference-workspace": {
      "types": "./dist/reference-workspace-bindings.d.ts",
      "import": "./dist/reference-workspace-bindings.js"
    },
    "./cli": {
      "import": "./dist/cli.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "create-next-hydra": "dist/cli.js"
  },
  "files": [
    "dist",
    "schema"
  ],
  "scripts": {
    "prebuild": "node scripts/clean-dist.js",
    "build": "tsc6 -p tsconfig.json",
    "compose": "pnpm --workspace-root exec turbo run build --filter=create-next-hydra && node dist/cli.js compose",
    "pack": "pnpm pack",
    "prepublishOnly": "pnpm run build",
    "publish:dry-run": "pnpm publish --access public --dry-run",
    "publish:public": "pnpm publish --access public",
    "test": "NODE_ENV=test vitest run --exclude 'tests/e2e/**'",
    "pretest:e2e": "pnpm run build",
    "test:e2e": "NODE_ENV=test vitest run tests/e2e",
    "typecheck": "tsc6 --noEmit -p tsconfig.json && tsc6 -p tsconfig.tests.json",
    "workspace:test": "node dist/test-reference-workspace.js",
    "workspace:lint": "node dist/lint-workspaces.js",
    "lint:staged": "pnpm run build && node dist/lint-workspaces.js --staged"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "@parcel/watcher": "^2.6.0",
    "effect": "catalog:",
    "jsonc-parser": "^3.3.1",
    "oxfmt": "0.61.0",
    "shadcn": "^4.16.2",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": "24.x"
  },
  "publishConfig": {
    "access": "public"
  }
}
