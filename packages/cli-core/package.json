{
  "name": "@repo/cli-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./private-dotenv": "./private-dotenv.ts",
    "./runtime-environment": "./runtime-environment.ts",
    "./runtime-environment-cli": "./runtime-environment-cli.ts"
  },
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "test": "vitest run",
    "typecheck": "pnpm --workspace-root exec tsc --noEmit --project packages/cli-core/tsconfig.json"
  },
  "devDependencies": {
    "@effect/platform-node": "catalog:",
    "@effect/vitest": "catalog:",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "effect": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "effect": "catalog:"
  }
}
