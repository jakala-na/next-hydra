NEXT_PUBLIC_WEB_URL=http://localhost:3000
CMS_HOMEPAGE_SLUG=homepage
CMS_REVALIDATION_SECRET=replace_with_at_least_32_random_characters

# Also copy your selected provider's environment variables into this app's
# .env.local. Provider configuration lives in packages/cms-*/keys.ts and,
# when selected, packages/auth-*/keys.ts.
