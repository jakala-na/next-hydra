{
  "name": "cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "git clean -xdf .cache .turbo node_modules",
    "cli": "jiti src/index.ts",
    "test": "NODE_ENV=test vitest run",
    "typecheck": "pnpm --workspace-root exec tsc --noEmit --project apps/cli/tsconfig.json"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "@repo/auth": "workspace:@repo/auth-workos@*",
    "@repo/commerce-provider": "workspace:@repo/commerce-commercetools@*",
    "@repo/cms": "workspace:@repo/cms-contentstack@*",
    "effect": "catalog:",
    "jiti": "^2.6.1"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
