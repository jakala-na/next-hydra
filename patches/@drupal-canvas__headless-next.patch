diff --git a/src/adapter.ts b/src/adapter.ts
index d5c4c6484b813f2817379832cd952df08a6851f0..db7a407890a2f12f51f335475f244b8b55e7fa0c 100644
--- a/src/adapter.ts
+++ b/src/adapter.ts
@@ -30,5 +30,5 @@ export const nextDraftAdapter: DraftServerAdapter = {
   // redirect() throws (its return type is never), which satisfies the
   // adapter's Response-returning contract; Next converts the control-flow
   // error into the redirect response.
-  redirect: (path) => redirect(path),
+  redirect: (path) => redirect(path as Parameters<typeof redirect>[0]),
 };
diff --git a/src/with-canvas.ts b/src/with-canvas.ts
--- a/src/with-canvas.ts
+++ b/src/with-canvas.ts
@@ -78,6 +78,11 @@ export interface WithCanvasOptions {
    * Default: process.cwd() (where `next build` runs).
    */
   projectRoot?: string;
+  /**
+   * The Next.js application root used to resolve Turbopack aliases.
+   * Default: process.cwd() (where `next build` or `next dev` runs).
+   */
+  appRoot?: string;
 }
 
 /**
@@ -126,6 +131,7 @@ export function withCanvas(
         ? await nextConfig(phase, context)
         : nextConfig;
     const projectRoot = path.resolve(options.projectRoot ?? process.cwd());
+    const appRoot = path.resolve(options.appRoot ?? process.cwd());
     if (phase === PHASE_DEVELOPMENT_SERVER) {
       resolveDraftConfig();
     }
@@ -137,7 +143,7 @@ export function withCanvas(
       watchComponentRegistry(projectRoot);
     }
     const turbopackComponentRegistryPath = `./${path
-      .relative(projectRoot, componentRegistryPath)
+      .relative(appRoot, componentRegistryPath)
       .split(path.sep)
       .join('/')}`;
 
