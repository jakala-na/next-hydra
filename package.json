{
  "name": "next-hydra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/jakala-na/next-hydra"
  },
  "scripts": {
    "boundaries": "turbo run boundaries",
    "build": "turbo run build workspace:build --filter=create-next-hydra",
    "bump-deps": "npx npm-check-updates --deep -u -x react-day-picker",
    "bump-ui": "npx shadcn@latest add --all --overwrite -c packages/design-system",
    "changeset": "changeset",
    "changeset:status": "changeset status --verbose",
    "clean": "git clean -xdf node_modules",
    "dev": "turbo run workspace:dev --filter=create-next-hydra",
    "e2e:env:split": "node scripts/split-e2e-github-env.ts",
    "format": "ultracite fix",
    "lint": "turbo run workspace:lint --filter=create-next-hydra",
    "prepare": "husky && effect-tsgo patch --typescript --oxlint",
    "publish:cli": "changeset publish",
    "registry:check": "node scripts/sync-registry-files.ts --check",
    "workspace:sync": "turbo run workspace:sync --filter=create-next-hydra",
    "workspace:check": "turbo run workspace:check --filter=create-next-hydra",
    "registry:sync": "node scripts/sync-registry-files.ts",
    "release:create-next-hydra": "node scripts/release-create-next-hydra.mjs",
    "release:create-next-hydra:dry-run": "node scripts/release-create-next-hydra.mjs --dry-run",
    "test": "turbo run test workspace:test --filter='./packages/*' --filter=@repo/e2e --continue=always",
    "test:environment": "node scripts/check-test-environments.ts",
    "test:e2e": "turbo run workspace:e2e",
    "test:e2e:affected": "turbo run workspace:e2e --affected",
    "translate": "turbo translate",
    "typecheck": "turbo run typecheck workspace:typecheck --filter=create-next-hydra",
    "version:cli": "changeset version"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "oxfmt --write"
    ],
    "*.{json,jsonc,css,scss,md,mdx}": [
      "oxfmt --write"
    ]
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@changesets/cli": "3.0.1",
    "@effect/tsgo": "0.36.5",
    "@oxlint/plugins": "1.78.0",
    "@repo/typescript-config": "workspace:*",
    "@turbo/gen": "^2.5.3",
    "@types/node": "catalog:",
    "@typescript/native": "catalog:typescript7",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "oxfmt": "0.61.0",
    "oxlint": "1.78.0",
    "oxlint-tsgolint": "7.0.2001",
    "portless": "0.15.6",
    "turbo": "2.10.13",
    "typescript": "catalog:",
    "ultracite": "7.10.0",
    "vitest": "catalog:"
  },
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": "24.x"
  }
}
